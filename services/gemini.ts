// AI integration removed. The project does not include client-side AI features.
// If you need server-side generation in future, implement a backend service
// that performs the external API call and returns validated JSON to the frontend.

export const generateSchemaFromPrompt = async (_prompt: string) => {
  throw new Error('AI integration disabled — no client-side AI in this project.');
};